//11_8_array

#include <iostream>

using namespace std;

int main()
{
    // Define an array of doubles of size 4
    double scores[4];

    cout << "Enter score from first judge: ";
    cin >> scores[0];
    cout << "Score is " << scores[0] << endl;

    cout << "Enter score from first judge: ";
    cin >> scores[1];
    cout << "Score is " << scores[1] << endl;

    cout << "Enter score from first judge: ";
    cin >> scores[2];
    cout << "Score is " << scores[2] << endl;

    cout << "Enter score from first judge: ";
    cin >> scores[3];
    cout << "Score is " << scores[3] << endl;

    return 0;
}
